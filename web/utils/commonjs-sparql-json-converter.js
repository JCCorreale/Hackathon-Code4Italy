// Generated by CoffeeScript 1.7.1
var converter, fs;

fs = require('fs');

converter = function(input, outputFilename) {
  var e, k, n, res, source, v, _i, _len, _ref;
  if (typeof input === 'string') {
    source = require(input);
  } else {
    source = input;
  }
  res = [];
  _ref = source.results.bindings;
  for (_i = 0, _len = _ref.length; _i < _len; _i++) {
    e = _ref[_i];
    n = {};
    for (k in e) {
      v = e[k];
      n[k] = v.value;
    }
    res.push(n);
  }
  fs.writeFile(outputFilename, JSON.stringify(res), function() {});
};

module.exports = converter;
